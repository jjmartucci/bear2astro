{
  "engines": {
    "node": ">=20.6.0"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "turndown": "^7.2.0"
  },
  "scripts": {
    "convert": "rm -rf ./out && node --env-file=.env convert.js"
  }
}
